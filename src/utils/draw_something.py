import matplotlib.pyplot as plt
import numpy as np

plt.rcParams["font.sans-serif"] = ["SimHei"]  # 用来正常显示中文标签
plt.rcParams["axes.unicode_minus"] = False  # 用来正常显示负号

# 识别其中的数据，并给我对应的python代码用来作图，并且每一个数据点都要标清楚数据在图上。
# 这两行代码用来正常显示中文。。

